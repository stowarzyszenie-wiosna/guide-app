<%= form_for @group, :html => { :class => "form-horizontal group" } do |f| %>

  <% if @group.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @group.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :site_url, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :site_url, :class => 'form-control' %>
    </div>
    <%=f.error_span(:site_url) %>
  </div>
  <div class="form-group">
    <%= f.label :scenario_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :scenario_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:scenario_id) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                groups_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
