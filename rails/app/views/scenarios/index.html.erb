<%- model_class = Scenario -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @scenarios.each do |scenario| %>
      <tr>
        <td><%= link_to scenario.id, scenario_path(scenario) %></td>
        <td><%= scenario.name %></td>
        <td><%=l scenario.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_scenario_path(scenario), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      scenario_path(scenario),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>




<%= link_to t('.new', :default => t("helpers.links.new")),
            new_scenario_path,
            :class => 'btn btn-primary custom-new-scenario' %>

<form class="form-horizontal scenario custom-scenario" id="new_scenario" action="/scenarios" accept-charset="UTF-8" method="post"><input name="utf8" value="✓" type="hidden"><input name="authenticity_token" value="gpHxZfO85mZkubf5u44ddiiebRGr+crdAp1BEn4vu06JIzurgfp/4FoJK2M0I8zFaCJDFAWdlZF6TyopPWAtrA==" type="hidden">


  <div class="form-group">
    <div class="col-lg-10">
      <input class="form-control" name="scenario[name]" id="scenario_name" type="text">
    </div>
    
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <input name="commit" value="Create Scenario" class="btn btn-primary" type="submit">
      <a class="btn btn-default custom-cancel-adding" href="/scenarios">Cancel</a>
    </div>
  </div>

</form>
